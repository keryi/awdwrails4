<%= form_for @photo, multipart: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_field :title %>

  <%= f.file_field :uploaded_file %>
  <%= f.submit 'Add to gallery' %>
<% end %>
